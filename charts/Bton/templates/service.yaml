apiVersion: v1
kind: Service
metadata:
  name: {{ include "Bton.fullname" . }}
  labels:
    {{- include "Bton.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "Bton.selectorLabels" . | nindent 4 }}
